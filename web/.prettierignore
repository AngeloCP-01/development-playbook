.next
node_modules
pnpm-lock.yaml

# Markdown is documentation, not code — Prettier's prose/emphasis rules are
# opinionated formatting, not correctness, so it is deliberately not checked.
# (reference/glossary.md is also generated and must not be reformatted.)
*.md

# Generated by pnpm gen:highlighted; do not hand-edit. Unlike the .md
# generators above, this one produces real TypeScript, so Prettier reaching
# it would fight the generator on every commit — the committed file and
# generate-highlighted.test.ts's snapshot would drift out from under each
# other, quote style first.
src/lib/cheatsheets/highlighted.generated.ts
