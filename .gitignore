# OS
.DS_Store

# Browser-automation artifacts. These landed in the repo root during
# development more than once; ignoring them stops it recurring.
.playwright-mcp/
*.png
!web/public/**/*.png

# Gathered cheatsheet originals. The converted WebP in web/public/reference/ is
# what the site serves and what is committed; these run 1-4MB each and git keeps
# every version forever. Provenance lives in reference/cheatsheet-sources.md.
reference/*.gif
reference/*.jpeg
reference/*.jpg
